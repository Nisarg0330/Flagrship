// /docs is the quickstart. A server redirect does not exist in a static
// export, and a client-side one would flash; rendering it directly is both.
export { default } from './quickstart/page';
export const metadata = { title: 'Docs · Flagrship' };
